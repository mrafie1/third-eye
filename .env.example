# Keep this key on the backend machine; never copy it to the hat.
GEMINI_API_KEY=replace_with_a_new_rotated_key
GEMINI_VISION_MODEL=gemini-3-flash-preview

# Set this on the QNX device when the backend is on another machine.
THIRD_EYE_SERVER_URL=http://192.168.1.100:8000
